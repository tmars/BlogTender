<div class="content-wiev_tab">
<!-- <h2>Статьи</h2> -->
<div class="section">

  {% for mypost in posts %}
  <div class="box" style="display:{% if loop.first %}block;{% else %}none;{% endif %}">
    <div>
      <img alt="{{ mypost.title }}" style="max-width: 636px; cursor:pointer;" src="{{ mypost.preview | apply_filter('post_image') }}" onclick="window.location.href='{{ path('show_post', {id: mypost.id}) }}'"/>
      <div id="tab_cont" class="long_post">
        <a href="{{ path('show_post', {id: mypost.id}) }}"><h3>{{ mypost.title }}</h3></a>
		<a href="" style="font-size:10px;">Как сюда попасть?</a>
        <p>{{ mypost.textPreview }}</p>
     <noindex> <!--   <a class="but-yellow" href="{{ path('show_post', {id: mypost.id}) }}">прочитать</a> --> </noindex>
      </div>
    </div>
  </div>
  {% endfor %}


  <div class="tbl">
  <ul class="tabs">
    {% for mypost in posts %}
    <li{% if loop.first %} class="current"{% endif %}><span>{{ mypost.title }}</span></li>
    {% endfor %}
  </ul>
  </div>

</div>
</div><!-- end content-wiev_tab-->